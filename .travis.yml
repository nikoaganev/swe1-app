language: python

python:
  - "3.8"


before_script:
   - pip install -r requirements.txt

script:
  # Run Black formatter
  - black --check --diff .

  # Run Flake8 linter
  - flake8 --extend-ignore E501,F401,F811

  # Run Django tests
  - coverage run manage.py test
